# GraphQL Profile

A simple profile dashboard that authenticates with Reboot GraphQL and displays user stats, XP progress, audits, and SVG graphs.

- Hosted: https://slothping.github.io/graphql/
- Login via Reboot signin endpoint
- Uses JWT for authenticated GraphQL queries
